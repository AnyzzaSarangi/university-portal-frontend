## ResultSet
- for every statement object , we should have resultset object
- extract data from every row
- object set always points to before the first row 
- ResultSet is of two types : scrollable and non-scrollable
- ResultSet Concurrency

